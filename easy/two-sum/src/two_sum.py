from typing import List

def two_sum(nums: List[int], target: int) -> List[int]:
	"""
	Finds the indices of the two numbers in the list that add up to the target.
	@param nums The list of numbers that may be used as addends. The list is
	  guaranteed to have at least two elements, and is guaranteed to contain
	  exactly one solution.
	@param target The sum that the two numbers must equal.
	@return The indices of the two numbers that add up to the target. The
	  indices may be returned in any order.
	"""
